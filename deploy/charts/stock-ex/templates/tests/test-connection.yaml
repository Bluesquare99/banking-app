apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stock-ex.fullname" . }}-test-connection"
  labels:
    {{- include "stock-ex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stock-ex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
