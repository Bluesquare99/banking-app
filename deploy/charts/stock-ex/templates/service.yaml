apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-ex.fullname" . }}
  labels:
    {{- include "stock-ex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stock-ex.selectorLabels" . | nindent 4 }}
